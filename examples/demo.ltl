// examples/demo.ltl — runnable demo for ltl_flatbuf_demo

import std::io
use ltl_flatbuf_demo as lib

fn main() {
    io::println("ltl_flatbuf_demo demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}