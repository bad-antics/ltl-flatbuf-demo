// ltl-flatbuf-demo/main.ltl
//
// FlatBuffers offset-table reader — zero-copy.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-flatbuf-demo v0.1.0")
    io::println("FlatBuffers offset-table reader — zero-copy.")
    0
}
